import{NextResponse}from"next/server";import{ensureSchema,getSql,first}from"@/lib/db";import{requireAuth}from"@/lib/auth";
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){try{const denied=await requireAuth(req);if(denied)return denied;const{id}=await params;const b=await req.json();const allowed=["Draft","Sent","Paid","Overdue","Cancelled"];if(!allowed.includes(b.status))return NextResponse.json({error:"Invalid status"},{status:400});await ensureSchema();const r=await getSql()`UPDATE invoices SET status=${b.status},updated_at=NOW() WHERE id=${id} RETURNING *`;const row=first(r);return row?NextResponse.json(row):NextResponse.json({error:"Not found"},{status:404});}catch(e){console.error("[invoice status]",e);return NextResponse.json({error:"Unable to update invoice"},{status:500});}}
